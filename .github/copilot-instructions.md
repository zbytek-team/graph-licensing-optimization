This repository uses a single package `glopt` under `src/`.
Place all source files inside this package and prefer relative imports within it.
Outputs from experiments are stored in the `runs/` directory.
