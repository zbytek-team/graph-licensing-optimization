import { Card, CardContent } from "@/components/ui/card";

export default function GraphView() {
  return (
    <Card className="h-full">
      <CardContent className="p-4 h-full"></CardContent>
    </Card>
  );
}
